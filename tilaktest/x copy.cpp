#include<bits/stdc++.h>
using namespace std;
vector<long long int> find(vector<int> &v, int n){
    long long int miny = 1e9 + 7;
    unordered_map<int,vector<int>> mp;
    for(int i = 0; i<n; i++){
        if(v[i] != =1){}
    }
}
int solve(vector<int> &v, int n){
    int cur = 0, op = 0;
    while(n--){
        vector<int> ind = find(v,n);
    }
}

int main(){
    return 0;
}